package com.Applicant.practice.CoreJava.takeUForward.BruteForce;

import java.util.Arrays;

public class RotateArray {
    public static void main(String[] args) {
        String s="Kundan";
        StringBuffer st=new StringBuffer(s);
        StringBuffer reverse = st.reverse();
       // System.out.println(reverse);
        long unsignedValue = 4294967295L; // Large number stored in long
        int signedConverted = (int) unsignedValue; // Signed interpretation
        System.out.println(signedConverted); // Output may be negative due to overflow

        //System.out.println(signum);

    }
}
